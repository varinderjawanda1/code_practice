---
- name: install httpd
  hosts: all
  become: yes
  tasks:
    - name : Install latest httpd version
    yum:
    name: httpd
    state: latest
    - name: Start httpd
      service:
        name: httpd
        enable: true
        starte: started
